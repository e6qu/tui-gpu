[package]
name = "terminal-session"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
libc = "0.2"
nix = { version = "0.28", default-features = false, features = ["process", "term", "fs", "signal"] }
serde = { version = "1.0", features = ["derive"] }
