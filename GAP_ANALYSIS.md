## GAP ANALYSIS
- Need to integrate `alacritty_terminal` for terminal grids; current terminal session only spawns PTY/reads raw bytes.
- Renderer still lacks glyph cache/text rendering.
- Layout compiler emits flat nodes only; no nested/z-order metadata.
- Runtime service/bus + multi-transport API still missing.
- CI/visual testing harness still missing.
