## GAP ANALYSIS
- Terminal session currently only spawns PTY + shell; libvterm integration and buffer exposure still missing.
- Renderer still lacks glyph cache/text rendering; panes show placeholder colors.
- Layout compiler emits flat node list; no nested structure/z-order metadata.
- Runtime service/bus + multi-transport API not implemented.
- CI/visual testing harness (tmux/screenshots) still missing.
