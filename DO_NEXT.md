## DO NEXT
- Integrate `alacritty_terminal` into `terminal-session` (replace libvterm plan) and expose buffer snapshots/tests.
- Render terminal buffer in renderer (glyph cache/text rendering).
- Enhance layout compiler (nested nodes/z-order) feeding Taffy tree.
- Build runtime API/event bus atop runtime-core.
